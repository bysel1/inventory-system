# Tkinter TTK Inventory Management System

A simple inventory management application using Python, Tkinter (TTK), and SQLite.

### Features
*   Add products
*   View inventory
*   Delete products

### Run
1. Install requirements: `pip install -r requirements.txt`
2. Run the app: `python main.py`
